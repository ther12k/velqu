// @generated by crates/q-bench-support/src/bin/codec_bench/generator.rs — DO NOT EDIT.
// Corpus: M25-002-A/B (small_user, nested_order, records100, records1000, pad_256, pad_1k, pad_16k, pad_64k, opt_null, problem_shape).
// Regenerate: cargo run -p q-bench-support --bin q-codec-bench -- --emit-generated crates/q-bench-support/src/bin/codec_bench/generated.rs
// Fused decode/validate projections over the parsed JSON value. Error paths,
// codes, and messages are byte-identical to q_schema_runtime::validate
// (Source::Body); `supports` fails closed outside the supported subset.

use q_schema_runtime::{FieldError, SchemaIr};
use serde_json::{Map, Number, Value};

#[rustfmt::skip]
fn fe(path: &str, code: &str, message: String) -> FieldError {
    FieldError { path: path.to_string(), code: code.to_string(), message }
}

#[rustfmt::skip]
fn join_path(base: &str, key: &str) -> String {
    if base.is_empty() { key.to_string() } else { format!("{}.{}", base, key) }
}

#[rustfmt::skip]
pub fn supports(ir: &SchemaIr) -> bool {
    match ir {
        SchemaIr::String { pattern, format, .. } => pattern.is_none() && format.is_none(),
        SchemaIr::Integer { .. } | SchemaIr::Number { .. } | SchemaIr::Boolean => true,
        SchemaIr::Optional { inner, .. } | SchemaIr::Nullable { inner } => supports(inner),
        SchemaIr::Array { items, .. } => supports(items),
        SchemaIr::Object { properties, .. } => properties.values().all(|b| supports(b)),
        SchemaIr::Fallback { inner: Some(inner), .. } => supports(inner),
        _ => false,
    }
}

#[rustfmt::skip]
pub fn decode(name: &str, v: &Value) -> Option<Result<Value, Vec<FieldError>>> {
    match name {
        "small_user" => Some(decode_small_user(v)),
        "nested_order" => Some(decode_nested_order(v)),
        "records100" => Some(decode_records100(v)),
        "records1000" => Some(decode_records1000(v)),
        "pad_256" => Some(decode_pad_256(v)),
        "pad_1k" => Some(decode_pad_1k(v)),
        "pad_16k" => Some(decode_pad_16k(v)),
        "pad_64k" => Some(decode_pad_64k(v)),
        "opt_null" => Some(decode_opt_null(v)),
        "problem_shape" => Some(decode_problem_shape(v)),
        _ => None,
    }
}

#[rustfmt::skip]
#[allow(clippy::too_many_lines)]
pub fn decode_small_user(v: &Value) -> Result<Value, Vec<FieldError>> {
    let p0: String = String::new();
    let r1: Result<Value, Vec<FieldError>> = (|| {
        let obj = match v.as_object() {
            Some(o) => o,
            None => return Err(vec![fe(&p0, "type", "expected object".to_string())]),
        };
        let mut errors: Vec<FieldError> = Vec::new();
        for key in obj.keys() {
            match key.as_str() {
            "active" | "id" | "name" | "nickname" | "tag" => {}
                _ => errors.push(fe(&join_path(&p0, key), "additional", "unknown field".to_string())),
            }
        }
        if !obj.contains_key("active") {
            errors.push(fe(&join_path(&p0, "active"), "required", "missing required field".to_string()));
        }
        if !obj.contains_key("id") {
            errors.push(fe(&join_path(&p0, "id"), "required", "missing required field".to_string()));
        }
        if !obj.contains_key("name") {
            errors.push(fe(&join_path(&p0, "name"), "required", "missing required field".to_string()));
        }
        if !errors.is_empty() { return Err(errors); }
        let mut out = Map::new();
        let p4: String = join_path(&p0, "active");
        if let Some(v5) = obj.get("active") {
            let r6: Result<Value, Vec<FieldError>> = match v5.as_bool() {
                Some(b) => Ok(Value::Bool(b)),
                None => Err(vec![fe(&p4, "type", "expected boolean".to_string())]),
            };
            match r6 { Ok(nv) => { out.insert("active".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
        }
        let p9: String = join_path(&p0, "id");
        if let Some(v10) = obj.get("id") {
            let r11: Result<Value, Vec<FieldError>> = (|| {
                let n12 = match v10.as_i64() {
                    Some(n) => n,
                    None => return Err(vec![fe(&p9, "type", "expected integer".to_string())]),
                };
                if n12 < 0 {
                    return Err(vec![fe(&p9, "minimum", "must be at least 0".to_string())]);
                }
                Ok(Value::Number(Number::from(n12)))
            })();
            match r11 { Ok(nv) => { out.insert("id".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
        }
        let p15: String = join_path(&p0, "name");
        if let Some(v16) = obj.get("name") {
            let r17: Result<Value, Vec<FieldError>> = (|| {
                let s18 = match v16.as_str() {
                    Some(s) => s,
                    None => return Err(vec![fe(&p15, "type", "expected string".to_string())]),
                };
                if (s18.len() as u64) < 1 {
                    return Err(vec![fe(&p15, "minLength", "must be at least 1 characters".to_string())]);
                }
                if (s18.len() as u64) > 64 {
                    return Err(vec![fe(&p15, "maxLength", "must be at most 64 characters".to_string())]);
                }
                Ok(Value::String(s18.to_string()))
            })();
            match r17 { Ok(nv) => { out.insert("name".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
        }
        let p19: String = join_path(&p0, "nickname");
        if let Some(v20) = obj.get("nickname") {
            if v20.is_null() {
                out.insert("nickname".to_string(), Value::Null);
            } else {
                let r21: Result<Value, Vec<FieldError>> = (|| {
                    if v20.is_null() { return Ok(Value::Null); }
                    let r22: Result<Value, Vec<FieldError>> = (|| {
                        let s23 = match v20.as_str() {
                            Some(s) => s,
                            None => return Err(vec![fe(&p19, "type", "expected string".to_string())]),
                        };
                        if (s23.len() as u64) > 32 {
                            return Err(vec![fe(&p19, "maxLength", "must be at most 32 characters".to_string())]);
                        }
                        Ok(Value::String(s23.to_string()))
                    })();
                    r22
                })();
                match r21 { Ok(nv) => { out.insert("nickname".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
            }
        }
        let p24: String = join_path(&p0, "tag");
        if let Some(v25) = obj.get("tag") {
            if v25.is_null() {
                out.insert("tag".to_string(), Value::String("none".to_string()));
            } else {
                let r26: Result<Value, Vec<FieldError>> = (|| {
                    let s27 = match v25.as_str() {
                        Some(s) => s,
                        None => return Err(vec![fe(&p24, "type", "expected string".to_string())]),
                    };
                    if (s27.len() as u64) < 1 {
                        return Err(vec![fe(&p24, "minLength", "must be at least 1 characters".to_string())]);
                    }
                    if (s27.len() as u64) > 16 {
                        return Err(vec![fe(&p24, "maxLength", "must be at most 16 characters".to_string())]);
                    }
                    Ok(Value::String(s27.to_string()))
                })();
                match r26 { Ok(nv) => { out.insert("tag".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
            }
        } else {
            out.insert("tag".to_string(), Value::String("none".to_string()));
        }
        if !errors.is_empty() { return Err(errors); }
        Ok(Value::Object(out))
    })();
    r1
}

#[rustfmt::skip]
#[allow(clippy::too_many_lines)]
pub fn decode_nested_order(v: &Value) -> Result<Value, Vec<FieldError>> {
    let p0: String = String::new();
    let r28: Result<Value, Vec<FieldError>> = (|| {
        let obj = match v.as_object() {
            Some(o) => o,
            None => return Err(vec![fe(&p0, "type", "expected object".to_string())]),
        };
        let mut errors: Vec<FieldError> = Vec::new();
        for key in obj.keys() {
            match key.as_str() {
            "meta" | "wrapper" => {}
                _ => errors.push(fe(&join_path(&p0, key), "additional", "unknown field".to_string())),
            }
        }
        if !obj.contains_key("meta") {
            errors.push(fe(&join_path(&p0, "meta"), "required", "missing required field".to_string()));
        }
        if !obj.contains_key("wrapper") {
            errors.push(fe(&join_path(&p0, "wrapper"), "required", "missing required field".to_string()));
        }
        if !errors.is_empty() { return Err(errors); }
        let mut out = Map::new();
        let p31: String = join_path(&p0, "meta");
        if let Some(v32) = obj.get("meta") {
            let r33: Result<Value, Vec<FieldError>> = (|| {
                let obj = match v32.as_object() {
                    Some(o) => o,
                    None => return Err(vec![fe(&p31, "type", "expected object".to_string())]),
                };
                let mut errors: Vec<FieldError> = Vec::new();
                for key in obj.keys() {
                    match key.as_str() {
            "page" | "total" => {}
                        _ => errors.push(fe(&join_path(&p31, key), "additional", "unknown field".to_string())),
                    }
                }
                if !obj.contains_key("page") {
                    errors.push(fe(&join_path(&p31, "page"), "required", "missing required field".to_string()));
                }
                if !obj.contains_key("total") {
                    errors.push(fe(&join_path(&p31, "total"), "required", "missing required field".to_string()));
                }
                if !errors.is_empty() { return Err(errors); }
                let mut out = Map::new();
                let p36: String = join_path(&p31, "page");
                if let Some(v37) = obj.get("page") {
                    let r38: Result<Value, Vec<FieldError>> = (|| {
                        let n39 = match v37.as_i64() {
                            Some(n) => n,
                            None => return Err(vec![fe(&p36, "type", "expected integer".to_string())]),
                        };
                        if n39 < 0 {
                            return Err(vec![fe(&p36, "minimum", "must be at least 0".to_string())]);
                        }
                        Ok(Value::Number(Number::from(n39)))
                    })();
                    match r38 { Ok(nv) => { out.insert("page".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
                }
                let p42: String = join_path(&p31, "total");
                if let Some(v43) = obj.get("total") {
                    let r44: Result<Value, Vec<FieldError>> = (|| {
                        let n45 = match v43.as_i64() {
                            Some(n) => n,
                            None => return Err(vec![fe(&p42, "type", "expected integer".to_string())]),
                        };
                        if n45 < 0 {
                            return Err(vec![fe(&p42, "minimum", "must be at least 0".to_string())]);
                        }
                        Ok(Value::Number(Number::from(n45)))
                    })();
                    match r44 { Ok(nv) => { out.insert("total".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
                }
                if !errors.is_empty() { return Err(errors); }
                Ok(Value::Object(out))
            })();
            match r33 { Ok(nv) => { out.insert("meta".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
        }
        let p48: String = join_path(&p0, "wrapper");
        if let Some(v49) = obj.get("wrapper") {
            let r50: Result<Value, Vec<FieldError>> = (|| {
                let obj = match v49.as_object() {
                    Some(o) => o,
                    None => return Err(vec![fe(&p48, "type", "expected object".to_string())]),
                };
                let mut errors: Vec<FieldError> = Vec::new();
                for key in obj.keys() {
                    match key.as_str() {
            "inner" => {}
                        _ => errors.push(fe(&join_path(&p48, key), "additional", "unknown field".to_string())),
                    }
                }
                if !obj.contains_key("inner") {
                    errors.push(fe(&join_path(&p48, "inner"), "required", "missing required field".to_string()));
                }
                if !errors.is_empty() { return Err(errors); }
                let mut out = Map::new();
                let p53: String = join_path(&p48, "inner");
                if let Some(v54) = obj.get("inner") {
                    let r55: Result<Value, Vec<FieldError>> = (|| {
                        let obj = match v54.as_object() {
                            Some(o) => o,
                            None => return Err(vec![fe(&p53, "type", "expected object".to_string())]),
                        };
                        let mut errors: Vec<FieldError> = Vec::new();
                        for key in obj.keys() {
                            match key.as_str() {
            "list" => {}
                                _ => errors.push(fe(&join_path(&p53, key), "additional", "unknown field".to_string())),
                            }
                        }
                        if !obj.contains_key("list") {
                            errors.push(fe(&join_path(&p53, "list"), "required", "missing required field".to_string()));
                        }
                        if !errors.is_empty() { return Err(errors); }
                        let mut out = Map::new();
                        let p58: String = join_path(&p53, "list");
                        if let Some(v59) = obj.get("list") {
                            let r60: Result<Value, Vec<FieldError>> = (|| {
                                let arr = match v59.as_array() {
                                    Some(a) => a,
                                    None => return Err(vec![fe(&p58, "type", "expected array".to_string())]),
                                };
                                if (arr.len() as u64) < 1 {
                                    return Err(vec![fe(&p58, "minItems", "must have at least 1 items".to_string())]);
                                }
                                let mut items: Vec<Value> = Vec::with_capacity(arr.len());
                                for (i61, item62) in arr.iter().enumerate() {
                                    let p63: String = format!("{}[{}]", &p58, i61);
                                    let r64: Result<Value, Vec<FieldError>> = (|| {
                                        let obj = match item62.as_object() {
                                            Some(o) => o,
                                            None => return Err(vec![fe(&p63, "type", "expected object".to_string())]),
                                        };
                                        let mut errors: Vec<FieldError> = Vec::new();
                                        for key in obj.keys() {
                                            match key.as_str() {
            "id" | "qty" => {}
                                                _ => errors.push(fe(&join_path(&p63, key), "additional", "unknown field".to_string())),
                                            }
                                        }
                                        if !obj.contains_key("id") {
                                            errors.push(fe(&join_path(&p63, "id"), "required", "missing required field".to_string()));
                                        }
                                        if !obj.contains_key("qty") {
                                            errors.push(fe(&join_path(&p63, "qty"), "required", "missing required field".to_string()));
                                        }
                                        if !errors.is_empty() { return Err(errors); }
                                        let mut out = Map::new();
                                        let p67: String = join_path(&p63, "id");
                                        if let Some(v68) = obj.get("id") {
                                            let r69: Result<Value, Vec<FieldError>> = (|| {
                                                let s70 = match v68.as_str() {
                                                    Some(s) => s,
                                                    None => return Err(vec![fe(&p67, "type", "expected string".to_string())]),
                                                };
                                                if (s70.len() as u64) < 1 {
                                                    return Err(vec![fe(&p67, "minLength", "must be at least 1 characters".to_string())]);
                                                }
                                                if (s70.len() as u64) > 32 {
                                                    return Err(vec![fe(&p67, "maxLength", "must be at most 32 characters".to_string())]);
                                                }
                                                Ok(Value::String(s70.to_string()))
                                            })();
                                            match r69 { Ok(nv) => { out.insert("id".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
                                        }
                                        let p73: String = join_path(&p63, "qty");
                                        if let Some(v74) = obj.get("qty") {
                                            let r75: Result<Value, Vec<FieldError>> = (|| {
                                                let n76 = match v74.as_i64() {
                                                    Some(n) => n,
                                                    None => return Err(vec![fe(&p73, "type", "expected integer".to_string())]),
                                                };
                                                if n76 < 0 {
                                                    return Err(vec![fe(&p73, "minimum", "must be at least 0".to_string())]);
                                                }
                                                if n76 > 1000 {
                                                    return Err(vec![fe(&p73, "maximum", "must be at most 1000".to_string())]);
                                                }
                                                Ok(Value::Number(Number::from(n76)))
                                            })();
                                            match r75 { Ok(nv) => { out.insert("qty".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
                                        }
                                        if !errors.is_empty() { return Err(errors); }
                                        Ok(Value::Object(out))
                                    })();
                                    items.push(r64?);
                                }
                                Ok(Value::Array(items))
                            })();
                            match r60 { Ok(nv) => { out.insert("list".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
                        }
                        if !errors.is_empty() { return Err(errors); }
                        Ok(Value::Object(out))
                    })();
                    match r55 { Ok(nv) => { out.insert("inner".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
                }
                if !errors.is_empty() { return Err(errors); }
                Ok(Value::Object(out))
            })();
            match r50 { Ok(nv) => { out.insert("wrapper".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
        }
        if !errors.is_empty() { return Err(errors); }
        Ok(Value::Object(out))
    })();
    r28
}

#[rustfmt::skip]
#[allow(clippy::too_many_lines)]
pub fn decode_records100(v: &Value) -> Result<Value, Vec<FieldError>> {
    let p0: String = String::new();
    let r77: Result<Value, Vec<FieldError>> = (|| {
        let arr = match v.as_array() {
            Some(a) => a,
            None => return Err(vec![fe(&p0, "type", "expected array".to_string())]),
        };
        if (arr.len() as u64) < 1 {
            return Err(vec![fe(&p0, "minItems", "must have at least 1 items".to_string())]);
        }
        let mut items: Vec<Value> = Vec::with_capacity(arr.len());
        for (i78, item79) in arr.iter().enumerate() {
            let p80: String = format!("{}[{}]", &p0, i78);
            let r81: Result<Value, Vec<FieldError>> = (|| {
                let obj = match item79.as_object() {
                    Some(o) => o,
                    None => return Err(vec![fe(&p80, "type", "expected object".to_string())]),
                };
                let mut errors: Vec<FieldError> = Vec::new();
                for key in obj.keys() {
                    match key.as_str() {
            "active" | "id" | "name" | "qty" => {}
                        _ => errors.push(fe(&join_path(&p80, key), "additional", "unknown field".to_string())),
                    }
                }
                if !obj.contains_key("active") {
                    errors.push(fe(&join_path(&p80, "active"), "required", "missing required field".to_string()));
                }
                if !obj.contains_key("id") {
                    errors.push(fe(&join_path(&p80, "id"), "required", "missing required field".to_string()));
                }
                if !obj.contains_key("name") {
                    errors.push(fe(&join_path(&p80, "name"), "required", "missing required field".to_string()));
                }
                if !obj.contains_key("qty") {
                    errors.push(fe(&join_path(&p80, "qty"), "required", "missing required field".to_string()));
                }
                if !errors.is_empty() { return Err(errors); }
                let mut out = Map::new();
                let p84: String = join_path(&p80, "active");
                if let Some(v85) = obj.get("active") {
                    let r86: Result<Value, Vec<FieldError>> = match v85.as_bool() {
                        Some(b) => Ok(Value::Bool(b)),
                        None => Err(vec![fe(&p84, "type", "expected boolean".to_string())]),
                    };
                    match r86 { Ok(nv) => { out.insert("active".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
                }
                let p89: String = join_path(&p80, "id");
                if let Some(v90) = obj.get("id") {
                    let r91: Result<Value, Vec<FieldError>> = (|| {
                        let n92 = match v90.as_i64() {
                            Some(n) => n,
                            None => return Err(vec![fe(&p89, "type", "expected integer".to_string())]),
                        };
                        if n92 < 0 {
                            return Err(vec![fe(&p89, "minimum", "must be at least 0".to_string())]);
                        }
                        Ok(Value::Number(Number::from(n92)))
                    })();
                    match r91 { Ok(nv) => { out.insert("id".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
                }
                let p95: String = join_path(&p80, "name");
                if let Some(v96) = obj.get("name") {
                    let r97: Result<Value, Vec<FieldError>> = (|| {
                        let s98 = match v96.as_str() {
                            Some(s) => s,
                            None => return Err(vec![fe(&p95, "type", "expected string".to_string())]),
                        };
                        if (s98.len() as u64) < 1 {
                            return Err(vec![fe(&p95, "minLength", "must be at least 1 characters".to_string())]);
                        }
                        if (s98.len() as u64) > 64 {
                            return Err(vec![fe(&p95, "maxLength", "must be at most 64 characters".to_string())]);
                        }
                        Ok(Value::String(s98.to_string()))
                    })();
                    match r97 { Ok(nv) => { out.insert("name".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
                }
                let p101: String = join_path(&p80, "qty");
                if let Some(v102) = obj.get("qty") {
                    let r103: Result<Value, Vec<FieldError>> = (|| {
                        let n104 = match v102.as_i64() {
                            Some(n) => n,
                            None => return Err(vec![fe(&p101, "type", "expected integer".to_string())]),
                        };
                        if n104 < 0 {
                            return Err(vec![fe(&p101, "minimum", "must be at least 0".to_string())]);
                        }
                        Ok(Value::Number(Number::from(n104)))
                    })();
                    match r103 { Ok(nv) => { out.insert("qty".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
                }
                if !errors.is_empty() { return Err(errors); }
                Ok(Value::Object(out))
            })();
            items.push(r81?);
        }
        Ok(Value::Array(items))
    })();
    r77
}

#[rustfmt::skip]
#[allow(clippy::too_many_lines)]
pub fn decode_records1000(v: &Value) -> Result<Value, Vec<FieldError>> {
    let p0: String = String::new();
    let r105: Result<Value, Vec<FieldError>> = (|| {
        let arr = match v.as_array() {
            Some(a) => a,
            None => return Err(vec![fe(&p0, "type", "expected array".to_string())]),
        };
        if (arr.len() as u64) < 1 {
            return Err(vec![fe(&p0, "minItems", "must have at least 1 items".to_string())]);
        }
        let mut items: Vec<Value> = Vec::with_capacity(arr.len());
        for (i106, item107) in arr.iter().enumerate() {
            let p108: String = format!("{}[{}]", &p0, i106);
            let r109: Result<Value, Vec<FieldError>> = (|| {
                let obj = match item107.as_object() {
                    Some(o) => o,
                    None => return Err(vec![fe(&p108, "type", "expected object".to_string())]),
                };
                let mut errors: Vec<FieldError> = Vec::new();
                for key in obj.keys() {
                    match key.as_str() {
            "active" | "id" | "name" | "qty" => {}
                        _ => errors.push(fe(&join_path(&p108, key), "additional", "unknown field".to_string())),
                    }
                }
                if !obj.contains_key("active") {
                    errors.push(fe(&join_path(&p108, "active"), "required", "missing required field".to_string()));
                }
                if !obj.contains_key("id") {
                    errors.push(fe(&join_path(&p108, "id"), "required", "missing required field".to_string()));
                }
                if !obj.contains_key("name") {
                    errors.push(fe(&join_path(&p108, "name"), "required", "missing required field".to_string()));
                }
                if !obj.contains_key("qty") {
                    errors.push(fe(&join_path(&p108, "qty"), "required", "missing required field".to_string()));
                }
                if !errors.is_empty() { return Err(errors); }
                let mut out = Map::new();
                let p112: String = join_path(&p108, "active");
                if let Some(v113) = obj.get("active") {
                    let r114: Result<Value, Vec<FieldError>> = match v113.as_bool() {
                        Some(b) => Ok(Value::Bool(b)),
                        None => Err(vec![fe(&p112, "type", "expected boolean".to_string())]),
                    };
                    match r114 { Ok(nv) => { out.insert("active".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
                }
                let p117: String = join_path(&p108, "id");
                if let Some(v118) = obj.get("id") {
                    let r119: Result<Value, Vec<FieldError>> = (|| {
                        let n120 = match v118.as_i64() {
                            Some(n) => n,
                            None => return Err(vec![fe(&p117, "type", "expected integer".to_string())]),
                        };
                        if n120 < 0 {
                            return Err(vec![fe(&p117, "minimum", "must be at least 0".to_string())]);
                        }
                        Ok(Value::Number(Number::from(n120)))
                    })();
                    match r119 { Ok(nv) => { out.insert("id".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
                }
                let p123: String = join_path(&p108, "name");
                if let Some(v124) = obj.get("name") {
                    let r125: Result<Value, Vec<FieldError>> = (|| {
                        let s126 = match v124.as_str() {
                            Some(s) => s,
                            None => return Err(vec![fe(&p123, "type", "expected string".to_string())]),
                        };
                        if (s126.len() as u64) < 1 {
                            return Err(vec![fe(&p123, "minLength", "must be at least 1 characters".to_string())]);
                        }
                        if (s126.len() as u64) > 64 {
                            return Err(vec![fe(&p123, "maxLength", "must be at most 64 characters".to_string())]);
                        }
                        Ok(Value::String(s126.to_string()))
                    })();
                    match r125 { Ok(nv) => { out.insert("name".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
                }
                let p129: String = join_path(&p108, "qty");
                if let Some(v130) = obj.get("qty") {
                    let r131: Result<Value, Vec<FieldError>> = (|| {
                        let n132 = match v130.as_i64() {
                            Some(n) => n,
                            None => return Err(vec![fe(&p129, "type", "expected integer".to_string())]),
                        };
                        if n132 < 0 {
                            return Err(vec![fe(&p129, "minimum", "must be at least 0".to_string())]);
                        }
                        Ok(Value::Number(Number::from(n132)))
                    })();
                    match r131 { Ok(nv) => { out.insert("qty".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
                }
                if !errors.is_empty() { return Err(errors); }
                Ok(Value::Object(out))
            })();
            items.push(r109?);
        }
        Ok(Value::Array(items))
    })();
    r105
}

#[rustfmt::skip]
#[allow(clippy::too_many_lines)]
pub fn decode_pad_256(v: &Value) -> Result<Value, Vec<FieldError>> {
    let p0: String = String::new();
    let r133: Result<Value, Vec<FieldError>> = (|| {
        let obj = match v.as_object() {
            Some(o) => o,
            None => return Err(vec![fe(&p0, "type", "expected object".to_string())]),
        };
        let mut errors: Vec<FieldError> = Vec::new();
        for key in obj.keys() {
            match key.as_str() {
            "blob" | "id" | "label" => {}
                _ => errors.push(fe(&join_path(&p0, key), "additional", "unknown field".to_string())),
            }
        }
        if !obj.contains_key("blob") {
            errors.push(fe(&join_path(&p0, "blob"), "required", "missing required field".to_string()));
        }
        if !obj.contains_key("id") {
            errors.push(fe(&join_path(&p0, "id"), "required", "missing required field".to_string()));
        }
        if !obj.contains_key("label") {
            errors.push(fe(&join_path(&p0, "label"), "required", "missing required field".to_string()));
        }
        if !errors.is_empty() { return Err(errors); }
        let mut out = Map::new();
        let p136: String = join_path(&p0, "blob");
        if let Some(v137) = obj.get("blob") {
            let r138: Result<Value, Vec<FieldError>> = (|| {
                let s139 = match v137.as_str() {
                    Some(s) => s,
                    None => return Err(vec![fe(&p136, "type", "expected string".to_string())]),
                };
                if (s139.len() as u64) < 208 {
                    return Err(vec![fe(&p136, "minLength", "must be at least 208 characters".to_string())]);
                }
                if (s139.len() as u64) > 208 {
                    return Err(vec![fe(&p136, "maxLength", "must be at most 208 characters".to_string())]);
                }
                Ok(Value::String(s139.to_string()))
            })();
            match r138 { Ok(nv) => { out.insert("blob".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
        }
        let p142: String = join_path(&p0, "id");
        if let Some(v143) = obj.get("id") {
            let r144: Result<Value, Vec<FieldError>> = (|| {
                let n145 = match v143.as_i64() {
                    Some(n) => n,
                    None => return Err(vec![fe(&p142, "type", "expected integer".to_string())]),
                };
                if n145 < 0 {
                    return Err(vec![fe(&p142, "minimum", "must be at least 0".to_string())]);
                }
                Ok(Value::Number(Number::from(n145)))
            })();
            match r144 { Ok(nv) => { out.insert("id".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
        }
        let p148: String = join_path(&p0, "label");
        if let Some(v149) = obj.get("label") {
            let r150: Result<Value, Vec<FieldError>> = (|| {
                let s151 = match v149.as_str() {
                    Some(s) => s,
                    None => return Err(vec![fe(&p148, "type", "expected string".to_string())]),
                };
                if (s151.len() as u64) < 1 {
                    return Err(vec![fe(&p148, "minLength", "must be at least 1 characters".to_string())]);
                }
                if (s151.len() as u64) > 32 {
                    return Err(vec![fe(&p148, "maxLength", "must be at most 32 characters".to_string())]);
                }
                Ok(Value::String(s151.to_string()))
            })();
            match r150 { Ok(nv) => { out.insert("label".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
        }
        if !errors.is_empty() { return Err(errors); }
        Ok(Value::Object(out))
    })();
    r133
}

#[rustfmt::skip]
#[allow(clippy::too_many_lines)]
pub fn decode_pad_1k(v: &Value) -> Result<Value, Vec<FieldError>> {
    let p0: String = String::new();
    let r152: Result<Value, Vec<FieldError>> = (|| {
        let obj = match v.as_object() {
            Some(o) => o,
            None => return Err(vec![fe(&p0, "type", "expected object".to_string())]),
        };
        let mut errors: Vec<FieldError> = Vec::new();
        for key in obj.keys() {
            match key.as_str() {
            "blob" | "id" | "label" => {}
                _ => errors.push(fe(&join_path(&p0, key), "additional", "unknown field".to_string())),
            }
        }
        if !obj.contains_key("blob") {
            errors.push(fe(&join_path(&p0, "blob"), "required", "missing required field".to_string()));
        }
        if !obj.contains_key("id") {
            errors.push(fe(&join_path(&p0, "id"), "required", "missing required field".to_string()));
        }
        if !obj.contains_key("label") {
            errors.push(fe(&join_path(&p0, "label"), "required", "missing required field".to_string()));
        }
        if !errors.is_empty() { return Err(errors); }
        let mut out = Map::new();
        let p155: String = join_path(&p0, "blob");
        if let Some(v156) = obj.get("blob") {
            let r157: Result<Value, Vec<FieldError>> = (|| {
                let s158 = match v156.as_str() {
                    Some(s) => s,
                    None => return Err(vec![fe(&p155, "type", "expected string".to_string())]),
                };
                if (s158.len() as u64) < 976 {
                    return Err(vec![fe(&p155, "minLength", "must be at least 976 characters".to_string())]);
                }
                if (s158.len() as u64) > 976 {
                    return Err(vec![fe(&p155, "maxLength", "must be at most 976 characters".to_string())]);
                }
                Ok(Value::String(s158.to_string()))
            })();
            match r157 { Ok(nv) => { out.insert("blob".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
        }
        let p161: String = join_path(&p0, "id");
        if let Some(v162) = obj.get("id") {
            let r163: Result<Value, Vec<FieldError>> = (|| {
                let n164 = match v162.as_i64() {
                    Some(n) => n,
                    None => return Err(vec![fe(&p161, "type", "expected integer".to_string())]),
                };
                if n164 < 0 {
                    return Err(vec![fe(&p161, "minimum", "must be at least 0".to_string())]);
                }
                Ok(Value::Number(Number::from(n164)))
            })();
            match r163 { Ok(nv) => { out.insert("id".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
        }
        let p167: String = join_path(&p0, "label");
        if let Some(v168) = obj.get("label") {
            let r169: Result<Value, Vec<FieldError>> = (|| {
                let s170 = match v168.as_str() {
                    Some(s) => s,
                    None => return Err(vec![fe(&p167, "type", "expected string".to_string())]),
                };
                if (s170.len() as u64) < 1 {
                    return Err(vec![fe(&p167, "minLength", "must be at least 1 characters".to_string())]);
                }
                if (s170.len() as u64) > 32 {
                    return Err(vec![fe(&p167, "maxLength", "must be at most 32 characters".to_string())]);
                }
                Ok(Value::String(s170.to_string()))
            })();
            match r169 { Ok(nv) => { out.insert("label".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
        }
        if !errors.is_empty() { return Err(errors); }
        Ok(Value::Object(out))
    })();
    r152
}

#[rustfmt::skip]
#[allow(clippy::too_many_lines)]
pub fn decode_pad_16k(v: &Value) -> Result<Value, Vec<FieldError>> {
    let p0: String = String::new();
    let r171: Result<Value, Vec<FieldError>> = (|| {
        let obj = match v.as_object() {
            Some(o) => o,
            None => return Err(vec![fe(&p0, "type", "expected object".to_string())]),
        };
        let mut errors: Vec<FieldError> = Vec::new();
        for key in obj.keys() {
            match key.as_str() {
            "blob" | "id" | "label" => {}
                _ => errors.push(fe(&join_path(&p0, key), "additional", "unknown field".to_string())),
            }
        }
        if !obj.contains_key("blob") {
            errors.push(fe(&join_path(&p0, "blob"), "required", "missing required field".to_string()));
        }
        if !obj.contains_key("id") {
            errors.push(fe(&join_path(&p0, "id"), "required", "missing required field".to_string()));
        }
        if !obj.contains_key("label") {
            errors.push(fe(&join_path(&p0, "label"), "required", "missing required field".to_string()));
        }
        if !errors.is_empty() { return Err(errors); }
        let mut out = Map::new();
        let p174: String = join_path(&p0, "blob");
        if let Some(v175) = obj.get("blob") {
            let r176: Result<Value, Vec<FieldError>> = (|| {
                let s177 = match v175.as_str() {
                    Some(s) => s,
                    None => return Err(vec![fe(&p174, "type", "expected string".to_string())]),
                };
                if (s177.len() as u64) < 16336 {
                    return Err(vec![fe(&p174, "minLength", "must be at least 16336 characters".to_string())]);
                }
                if (s177.len() as u64) > 16336 {
                    return Err(vec![fe(&p174, "maxLength", "must be at most 16336 characters".to_string())]);
                }
                Ok(Value::String(s177.to_string()))
            })();
            match r176 { Ok(nv) => { out.insert("blob".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
        }
        let p180: String = join_path(&p0, "id");
        if let Some(v181) = obj.get("id") {
            let r182: Result<Value, Vec<FieldError>> = (|| {
                let n183 = match v181.as_i64() {
                    Some(n) => n,
                    None => return Err(vec![fe(&p180, "type", "expected integer".to_string())]),
                };
                if n183 < 0 {
                    return Err(vec![fe(&p180, "minimum", "must be at least 0".to_string())]);
                }
                Ok(Value::Number(Number::from(n183)))
            })();
            match r182 { Ok(nv) => { out.insert("id".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
        }
        let p186: String = join_path(&p0, "label");
        if let Some(v187) = obj.get("label") {
            let r188: Result<Value, Vec<FieldError>> = (|| {
                let s189 = match v187.as_str() {
                    Some(s) => s,
                    None => return Err(vec![fe(&p186, "type", "expected string".to_string())]),
                };
                if (s189.len() as u64) < 1 {
                    return Err(vec![fe(&p186, "minLength", "must be at least 1 characters".to_string())]);
                }
                if (s189.len() as u64) > 32 {
                    return Err(vec![fe(&p186, "maxLength", "must be at most 32 characters".to_string())]);
                }
                Ok(Value::String(s189.to_string()))
            })();
            match r188 { Ok(nv) => { out.insert("label".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
        }
        if !errors.is_empty() { return Err(errors); }
        Ok(Value::Object(out))
    })();
    r171
}

#[rustfmt::skip]
#[allow(clippy::too_many_lines)]
pub fn decode_pad_64k(v: &Value) -> Result<Value, Vec<FieldError>> {
    let p0: String = String::new();
    let r190: Result<Value, Vec<FieldError>> = (|| {
        let obj = match v.as_object() {
            Some(o) => o,
            None => return Err(vec![fe(&p0, "type", "expected object".to_string())]),
        };
        let mut errors: Vec<FieldError> = Vec::new();
        for key in obj.keys() {
            match key.as_str() {
            "blob" | "id" | "label" => {}
                _ => errors.push(fe(&join_path(&p0, key), "additional", "unknown field".to_string())),
            }
        }
        if !obj.contains_key("blob") {
            errors.push(fe(&join_path(&p0, "blob"), "required", "missing required field".to_string()));
        }
        if !obj.contains_key("id") {
            errors.push(fe(&join_path(&p0, "id"), "required", "missing required field".to_string()));
        }
        if !obj.contains_key("label") {
            errors.push(fe(&join_path(&p0, "label"), "required", "missing required field".to_string()));
        }
        if !errors.is_empty() { return Err(errors); }
        let mut out = Map::new();
        let p193: String = join_path(&p0, "blob");
        if let Some(v194) = obj.get("blob") {
            let r195: Result<Value, Vec<FieldError>> = (|| {
                let s196 = match v194.as_str() {
                    Some(s) => s,
                    None => return Err(vec![fe(&p193, "type", "expected string".to_string())]),
                };
                if (s196.len() as u64) < 65488 {
                    return Err(vec![fe(&p193, "minLength", "must be at least 65488 characters".to_string())]);
                }
                if (s196.len() as u64) > 65488 {
                    return Err(vec![fe(&p193, "maxLength", "must be at most 65488 characters".to_string())]);
                }
                Ok(Value::String(s196.to_string()))
            })();
            match r195 { Ok(nv) => { out.insert("blob".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
        }
        let p199: String = join_path(&p0, "id");
        if let Some(v200) = obj.get("id") {
            let r201: Result<Value, Vec<FieldError>> = (|| {
                let n202 = match v200.as_i64() {
                    Some(n) => n,
                    None => return Err(vec![fe(&p199, "type", "expected integer".to_string())]),
                };
                if n202 < 0 {
                    return Err(vec![fe(&p199, "minimum", "must be at least 0".to_string())]);
                }
                Ok(Value::Number(Number::from(n202)))
            })();
            match r201 { Ok(nv) => { out.insert("id".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
        }
        let p205: String = join_path(&p0, "label");
        if let Some(v206) = obj.get("label") {
            let r207: Result<Value, Vec<FieldError>> = (|| {
                let s208 = match v206.as_str() {
                    Some(s) => s,
                    None => return Err(vec![fe(&p205, "type", "expected string".to_string())]),
                };
                if (s208.len() as u64) < 1 {
                    return Err(vec![fe(&p205, "minLength", "must be at least 1 characters".to_string())]);
                }
                if (s208.len() as u64) > 32 {
                    return Err(vec![fe(&p205, "maxLength", "must be at most 32 characters".to_string())]);
                }
                Ok(Value::String(s208.to_string()))
            })();
            match r207 { Ok(nv) => { out.insert("label".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
        }
        if !errors.is_empty() { return Err(errors); }
        Ok(Value::Object(out))
    })();
    r190
}

#[rustfmt::skip]
#[allow(clippy::too_many_lines)]
pub fn decode_opt_null(v: &Value) -> Result<Value, Vec<FieldError>> {
    let p0: String = String::new();
    let r209: Result<Value, Vec<FieldError>> = (|| {
        let obj = match v.as_object() {
            Some(o) => o,
            None => return Err(vec![fe(&p0, "type", "expected object".to_string())]),
        };
        let mut errors: Vec<FieldError> = Vec::new();
        for key in obj.keys() {
            match key.as_str() {
            "a1" | "a2" | "b1" | "b2" | "id" | "n1" | "n2" | "tag" => {}
                _ => errors.push(fe(&join_path(&p0, key), "additional", "unknown field".to_string())),
            }
        }
        if !obj.contains_key("id") {
            errors.push(fe(&join_path(&p0, "id"), "required", "missing required field".to_string()));
        }
        if !errors.is_empty() { return Err(errors); }
        let mut out = Map::new();
        let p210: String = join_path(&p0, "a1");
        if let Some(v211) = obj.get("a1") {
            if v211.is_null() {
                out.insert("a1".to_string(), Value::Null);
            } else {
                let r212: Result<Value, Vec<FieldError>> = (|| {
                    let s213 = match v211.as_str() {
                        Some(s) => s,
                        None => return Err(vec![fe(&p210, "type", "expected string".to_string())]),
                    };
                    if (s213.len() as u64) > 8 {
                        return Err(vec![fe(&p210, "maxLength", "must be at most 8 characters".to_string())]);
                    }
                    Ok(Value::String(s213.to_string()))
                })();
                match r212 { Ok(nv) => { out.insert("a1".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
            }
        }
        let p214: String = join_path(&p0, "a2");
        if let Some(v215) = obj.get("a2") {
            if v215.is_null() {
                out.insert("a2".to_string(), Value::Null);
            } else {
                let r216: Result<Value, Vec<FieldError>> = (|| {
                    let s217 = match v215.as_str() {
                        Some(s) => s,
                        None => return Err(vec![fe(&p214, "type", "expected string".to_string())]),
                    };
                    if (s217.len() as u64) > 8 {
                        return Err(vec![fe(&p214, "maxLength", "must be at most 8 characters".to_string())]);
                    }
                    Ok(Value::String(s217.to_string()))
                })();
                match r216 { Ok(nv) => { out.insert("a2".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
            }
        }
        let p218: String = join_path(&p0, "b1");
        if let Some(v219) = obj.get("b1") {
            if v219.is_null() {
                out.insert("b1".to_string(), Value::String("bd1".to_string()));
            } else {
                let r220: Result<Value, Vec<FieldError>> = (|| {
                    let s221 = match v219.as_str() {
                        Some(s) => s,
                        None => return Err(vec![fe(&p218, "type", "expected string".to_string())]),
                    };
                    if (s221.len() as u64) > 8 {
                        return Err(vec![fe(&p218, "maxLength", "must be at most 8 characters".to_string())]);
                    }
                    Ok(Value::String(s221.to_string()))
                })();
                match r220 { Ok(nv) => { out.insert("b1".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
            }
        } else {
            out.insert("b1".to_string(), Value::String("bd1".to_string()));
        }
        let p222: String = join_path(&p0, "b2");
        if let Some(v223) = obj.get("b2") {
            if v223.is_null() {
                out.insert("b2".to_string(), Value::String("bd2".to_string()));
            } else {
                let r224: Result<Value, Vec<FieldError>> = (|| {
                    let s225 = match v223.as_str() {
                        Some(s) => s,
                        None => return Err(vec![fe(&p222, "type", "expected string".to_string())]),
                    };
                    if (s225.len() as u64) > 8 {
                        return Err(vec![fe(&p222, "maxLength", "must be at most 8 characters".to_string())]);
                    }
                    Ok(Value::String(s225.to_string()))
                })();
                match r224 { Ok(nv) => { out.insert("b2".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
            }
        } else {
            out.insert("b2".to_string(), Value::String("bd2".to_string()));
        }
        let p228: String = join_path(&p0, "id");
        if let Some(v229) = obj.get("id") {
            let r230: Result<Value, Vec<FieldError>> = (|| {
                let n231 = match v229.as_i64() {
                    Some(n) => n,
                    None => return Err(vec![fe(&p228, "type", "expected integer".to_string())]),
                };
                if n231 < 0 {
                    return Err(vec![fe(&p228, "minimum", "must be at least 0".to_string())]);
                }
                Ok(Value::Number(Number::from(n231)))
            })();
            match r230 { Ok(nv) => { out.insert("id".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
        }
        let p232: String = join_path(&p0, "n1");
        if let Some(v233) = obj.get("n1") {
            if v233.is_null() {
                out.insert("n1".to_string(), Value::Null);
            } else {
                let r234: Result<Value, Vec<FieldError>> = (|| {
                    let n235 = match v233.as_i64() {
                        Some(n) => n,
                        None => return Err(vec![fe(&p232, "type", "expected integer".to_string())]),
                    };
                    Ok(Value::Number(Number::from(n235)))
                })();
                match r234 { Ok(nv) => { out.insert("n1".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
            }
        }
        let p236: String = join_path(&p0, "n2");
        if let Some(v237) = obj.get("n2") {
            if v237.is_null() {
                out.insert("n2".to_string(), Value::Null);
            } else {
                let r238: Result<Value, Vec<FieldError>> = match v237.as_bool() {
                    Some(b) => Ok(Value::Bool(b)),
                    None => Err(vec![fe(&p236, "type", "expected boolean".to_string())]),
                };
                match r238 { Ok(nv) => { out.insert("n2".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
            }
        }
        let p239: String = join_path(&p0, "tag");
        if let Some(v240) = obj.get("tag") {
            if v240.is_null() {
                out.insert("tag".to_string(), Value::Null);
            } else {
                let r241: Result<Value, Vec<FieldError>> = (|| {
                    if v240.is_null() { return Ok(Value::Null); }
                    let r242: Result<Value, Vec<FieldError>> = (|| {
                        let s243 = match v240.as_str() {
                            Some(s) => s,
                            None => return Err(vec![fe(&p239, "type", "expected string".to_string())]),
                        };
                        if (s243.len() as u64) > 4 {
                            return Err(vec![fe(&p239, "maxLength", "must be at most 4 characters".to_string())]);
                        }
                        Ok(Value::String(s243.to_string()))
                    })();
                    r242
                })();
                match r241 { Ok(nv) => { out.insert("tag".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
            }
        }
        if !errors.is_empty() { return Err(errors); }
        Ok(Value::Object(out))
    })();
    r209
}

#[rustfmt::skip]
#[allow(clippy::too_many_lines)]
pub fn decode_problem_shape(v: &Value) -> Result<Value, Vec<FieldError>> {
    let p0: String = String::new();
    let r244: Result<Value, Vec<FieldError>> = (|| {
        let obj = match v.as_object() {
            Some(o) => o,
            None => return Err(vec![fe(&p0, "type", "expected object".to_string())]),
        };
        let mut errors: Vec<FieldError> = Vec::new();
        for key in obj.keys() {
            match key.as_str() {
            "detail" | "instance" | "status" | "title" | "type" => {}
                _ => errors.push(fe(&join_path(&p0, key), "additional", "unknown field".to_string())),
            }
        }
        if !obj.contains_key("status") {
            errors.push(fe(&join_path(&p0, "status"), "required", "missing required field".to_string()));
        }
        if !obj.contains_key("title") {
            errors.push(fe(&join_path(&p0, "title"), "required", "missing required field".to_string()));
        }
        if !obj.contains_key("type") {
            errors.push(fe(&join_path(&p0, "type"), "required", "missing required field".to_string()));
        }
        if !errors.is_empty() { return Err(errors); }
        let mut out = Map::new();
        let p245: String = join_path(&p0, "detail");
        if let Some(v246) = obj.get("detail") {
            if v246.is_null() {
                out.insert("detail".to_string(), Value::Null);
            } else {
                let r247: Result<Value, Vec<FieldError>> = (|| {
                    let s248 = match v246.as_str() {
                        Some(s) => s,
                        None => return Err(vec![fe(&p245, "type", "expected string".to_string())]),
                    };
                    if (s248.len() as u64) < 1 {
                        return Err(vec![fe(&p245, "minLength", "must be at least 1 characters".to_string())]);
                    }
                    if (s248.len() as u64) > 256 {
                        return Err(vec![fe(&p245, "maxLength", "must be at most 256 characters".to_string())]);
                    }
                    Ok(Value::String(s248.to_string()))
                })();
                match r247 { Ok(nv) => { out.insert("detail".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
            }
        }
        let p249: String = join_path(&p0, "instance");
        if let Some(v250) = obj.get("instance") {
            if v250.is_null() {
                out.insert("instance".to_string(), Value::Null);
            } else {
                let r251: Result<Value, Vec<FieldError>> = (|| {
                    let s252 = match v250.as_str() {
                        Some(s) => s,
                        None => return Err(vec![fe(&p249, "type", "expected string".to_string())]),
                    };
                    if (s252.len() as u64) > 256 {
                        return Err(vec![fe(&p249, "maxLength", "must be at most 256 characters".to_string())]);
                    }
                    Ok(Value::String(s252.to_string()))
                })();
                match r251 { Ok(nv) => { out.insert("instance".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
            }
        }
        let p255: String = join_path(&p0, "status");
        if let Some(v256) = obj.get("status") {
            let r257: Result<Value, Vec<FieldError>> = (|| {
                let n258 = match v256.as_i64() {
                    Some(n) => n,
                    None => return Err(vec![fe(&p255, "type", "expected integer".to_string())]),
                };
                if n258 < 400 {
                    return Err(vec![fe(&p255, "minimum", "must be at least 400".to_string())]);
                }
                if n258 > 599 {
                    return Err(vec![fe(&p255, "maximum", "must be at most 599".to_string())]);
                }
                Ok(Value::Number(Number::from(n258)))
            })();
            match r257 { Ok(nv) => { out.insert("status".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
        }
        let p261: String = join_path(&p0, "title");
        if let Some(v262) = obj.get("title") {
            let r263: Result<Value, Vec<FieldError>> = (|| {
                let s264 = match v262.as_str() {
                    Some(s) => s,
                    None => return Err(vec![fe(&p261, "type", "expected string".to_string())]),
                };
                if (s264.len() as u64) < 1 {
                    return Err(vec![fe(&p261, "minLength", "must be at least 1 characters".to_string())]);
                }
                if (s264.len() as u64) > 128 {
                    return Err(vec![fe(&p261, "maxLength", "must be at most 128 characters".to_string())]);
                }
                Ok(Value::String(s264.to_string()))
            })();
            match r263 { Ok(nv) => { out.insert("title".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
        }
        let p267: String = join_path(&p0, "type");
        if let Some(v268) = obj.get("type") {
            let r269: Result<Value, Vec<FieldError>> = (|| {
                let s270 = match v268.as_str() {
                    Some(s) => s,
                    None => return Err(vec![fe(&p267, "type", "expected string".to_string())]),
                };
                if (s270.len() as u64) < 1 {
                    return Err(vec![fe(&p267, "minLength", "must be at least 1 characters".to_string())]);
                }
                if (s270.len() as u64) > 128 {
                    return Err(vec![fe(&p267, "maxLength", "must be at most 128 characters".to_string())]);
                }
                Ok(Value::String(s270.to_string()))
            })();
            match r269 { Ok(nv) => { out.insert("type".to_string(), nv); } Err(mut e) => { errors.append(&mut e); } }
        }
        if !errors.is_empty() { return Err(errors); }
        Ok(Value::Object(out))
    })();
    r244
}
