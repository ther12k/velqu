[package]
name = "q-engine-quickjs"
version.workspace = true
edition.workspace = true
license.workspace = true

[features]
bench-instrumentation = ["q-bridge/bench-instrumentation"]
# diagnostic-only in-worker direct invocation harness (q-c3-decompose);
# never enabled by default or by any production crate
bench-direct = []

[dependencies]
rquickjs = { workspace = true }
q-engine = { workspace = true }
q-bridge = { workspace = true }
q-capabilities = { workspace = true }
q-capability-postgres = { workspace = true }
tokio = { workspace = true }
serde_json = { workspace = true }
bytes = { workspace = true }

[dev-dependencies]
bytes = { workspace = true }
