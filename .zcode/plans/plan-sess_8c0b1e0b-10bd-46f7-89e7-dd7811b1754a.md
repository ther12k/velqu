Implement BETA-017-G with conservative public benchmark wording based on current evidence:

1. Add `docs/beta/governance/BENCHMARK_WORDING.md` defining safe public claims: local Linux x86_64 glibc, release builds, loopback HTTP/1.1, frozen fixtures, pinned artifacts, raw evidence retained, warm p50/p95/p99 with five randomized repetitions, cold classes reported separately. Explicitly prohibit universal, production, cloud-cold-start, SLA, and unscoped speedup claims.
2. Update `docs/reports/final-report.md` and `docs/reports/release-gate-report.md` to replace unscoped marketing-style claims (`30–34× faster`, `13× lower`) with scoped evidence references; label historical comparator data as historical and not current gate evidence. Do not alter measured raw values or generated report data.
3. Update `docs/beta/governance/OPEN_DECISIONS.md`: mark OD-BETA-008 accepted on 2026-08-21 and add template-aligned record with context, options, decision, consequences, security/operations implications, and documentation updates.
4. Add OD-009 to `docs/open-decisions.md` for public benchmark wording.
5. Mark BETA-017-G `status: PASS`, append exact completion evidence, flip STATUS checkbox, regenerate indexes via `./scripts/spark-queue`.
6. Run `bun test`, `bun run typecheck`, `./scripts/validate-okf`, and benchmark evidence validation (`python3 scripts/validate-benchmark-evidence.py`). Review diff to ensure raw evidence and generated values were not hand-edited. Commit as `beta-017-g: public benchmark wording`, push, create PR with `Closes #622`, review/merge, resolve generated-index conflicts if master advances, and clean worktree.

Use conservative wording because no explicit user choice was returned: current evidence only, scoped and traceable; historical comparisons remain labeled historical. No universal or production claim will be invented.