# Current Reviewed Baseline

```text
Reviewed implementation commit: 4e6904951729ea14b48ca39a9564a950cc83e98e
Evidence capture checkpoint: 03cc48955c2f8b05c29cf6ca196572c67ed5dd2d
Release commit: BOUND_BY_RELEASE_PACKET_TO_CLEAN_HEAD
Source archive: generated by release/scripts after the clean commit is fixed
Git bundle:     generated by release/scripts after the clean commit is fixed
Target:         0.1.0-beta.1
Current gate:   G0 / M23R2 trusted numeric graph and evidence truth
```

## Baseline reconciliation

The reviewed implementation checkpoint is `4e69049`; benchmark evidence was captured at `03cc489`. Root indexes remain templates until `scripts/release-packet` binds them to the exact clean release commit.

## Closed G0 requirements and evidence

- The semantic function manifest is mandatory in current numeric mode.
- The serialized router is semantically verified, not merely bounds-checked.
- Current numeric startup loads the verified router without semantic reconstruction.
- RouteId, PolicyId, and SchemaId are complete operational identities on the request path.
- The public contract hash excludes implementation details and includes observable request/security semantics.
- Beta/production ledgers and review/evidence indexes identify the current commit and actual evidence through the release-packet binding rule.
- Canonical benchmark evidence is captured: five randomized warm repetitions, route-count/cold evidence, and allocator/startup profile; perf hardware counters remain unavailable on this host.

## Explicit carry-forward

Response-schema validation may remain on an optional slow path until M2.5, provided it is documented and does not weaken correctness.
