# QPack and Router Context

Current work must distinguish public contract identity from the internal execution graph. Current numeric artifacts require fail-before-ready manifests, dense IDs, and a compiler-emitted serialized router. The router must preserve method precedence, 404/405/Allow semantics, wildcard behavior, and route-specific parameter names.
