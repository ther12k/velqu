# Capabilities and Fetch Context

Capabilities are optional, dependency-resolved, versioned, lazy, and lifecycle-bounded. The minimal Web surface precedes fetch. Native fetch must propagate deadlines and AbortSignal, bound DNS/TLS/pools/streams, enforce redirect and SSRF policy, and shut down deterministically.
