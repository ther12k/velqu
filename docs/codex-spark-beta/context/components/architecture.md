# Architecture Context

Velqu is a cold-start-first TypeScript framework/runtime. Rust owns HTTP, routing, validation plans, resource limits, and native capabilities. QuickJS-NG runs trusted business logic. Bun is development/package/test tooling. The compiler must be static and side-effect-free.
