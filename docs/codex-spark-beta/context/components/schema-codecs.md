# Schema and Codec Context

M2.5 creates one canonical Schema IR that projects to generated input decoders, response encoders, RFC 9457 problems, Treaty, OpenAPI, and contract lock. Fallback paths must be explicit and visible. Choose codec strategies from measured evidence, not preference.
