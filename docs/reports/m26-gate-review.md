# M26 Gate Review

- Candidate commit: `9aa16be` (reviewed implementation); release packet
  bound to the gate commit generated by `scripts/release-packet`.
- Dependency closure: `M26-001-Z` through `M26-010-Z` all `PASS`
  (task ledger rows M26-001…M26-010 PASS). Per-parent evidence packets
  and their merged commits: `M26-001-Z` `a61e6c4` (format + ADR-0024/
  0025 acceptance), `M26-002-Z` `70ada0d` (runtime/bytecode
  fingerprint), `M26-003-Z` `6d3860c` (dense/graph section encoding,
  offsets, bounds, execution-integrity binding), `M26-004-Z` `742c5d1`
  (raw bytecode embedding, single-decode cache, embedded prelude),
  `M26-005-Z` `5f55113` (PackBytes reader: Mapped/Embedded/Owned,
  checked-add bounds, one-unsafe policy), `M26-006-Z` `36419e4`
  (ADR-0026 integrity vs authenticity, Ed25519 detached signatures,
  trust config), `M26-007-Z` `a548568` (reproducible release packs),
  `M26-008-Z` `3f7fd2c` (v1 compatibility + migration tool),
  `M26-009-Z` `bcae745` (shared/standalone deployment artifacts,
  ADR-0027 sidecars), `M26-010-Z` `0f2eb01` (route-count cold-start
  evidence closure).
- Clean `./scripts/verify`: PASS — fmt, clippy `-D warnings`, workspace
  Rust tests (q-pack 96, q-engine-quickjs 98, q-schema-runtime 67,
  q-router 15, velqu-runtime 30), release builds, typecheck, proof
  build, Bun 125/125, OKF validation, benchmark artifact validation.
- Raw-to-report parity re-checked fresh at the candidate commit:
  all 20 route-count cells × 5 metrics (p50/p95/p99, rssP50/rssP95)
  recomputed exactly from the 2,000-row canonical JSONL
  (`m26-010-d` run) using the harness's nearest-rank method; zero
  failure rows; 1,000 rows carry startup-stage traces.
- Artifact parity: all 12 `benchmarks/manifest.json` artifact hashes
  match current files (including `qRuntimeRelease` vs the
  verify-rebuilt binary); all 10 route-count pack hashes embedded in
  the summary match `benchmarks/raw/packs/`. The summary's
  `binaryHashes` value intentionally self-identifies the
  benchmark-run build and differs from the manifest's canonical
  verify-reproducible build; the division is documented in the
  M26-010-D record and is not a parity failure.
- Honest disclosure (carried-forward limitation, owner-visible): 
  `PACK_FORMAT_CURRENT` remains pinned to legacy v1 by the test
  `current_mode_is_pinned_until_native_v2_lands`. The compiler emits
  `formatVersion: 1` and the production default load path parses JSON
  packs, so the M25-escalated route-count cold-start limitation is
  **not yet mitigated in the default path**: the canonical M26-010
  ladder (measuring the v1 path) shows 25-route p50 6.143 ms (small-
  app budget preserved, fastest candidate) but 1,000-route p50
  82.96 ms and 10,000-route p50 926.3 ms with `pack.load` at ~94–97%
  — JSON pack parsing, not routing or JS. ADR-0024 migration rule 2
  anticipated the CURRENT flip landing atomically with the M26-003
  native adapter; the adapter landed but the flip did not, and no M26
  packet required it. The v2 binary sections, reader, bytecode path,
  integrity, reproducibility, migration tooling, and deployment modes
  all exist behind tests. Flipping the default requires an owner-
  authorized follow-up packet (one pinned constant + producer/runtime
  adapter flip + tests per ADR-0024); it is not patched silently
  inside this gate review and is recorded in both root indexes as an
  open item.
- No other unresolved M26 P0/P1 criteria. No unsupported performance
  claims; normative targets and measured results kept separate; the
  ladder makes no slope-fix claim for v2.
- Decision: M26 gate PASS; issue #629 may close.
