# M25 Gate Review

- Candidate commit: `d61096c` (reviewed implementation); release packet
  bound to the gate commit generated by `scripts/release-packet`.
- Dependency closure: `M25-001-Z` through `M25-010-Z` all `PASS`
  (Schema IR v2, strategy benchmark, params/query/header decoders,
  body decoders, response encoders, RFC 9457 problem encoders,
  explicit fallback paths, Treaty/OpenAPI/lock unification, codec
  fuzzing/differential tests, performance + cold-start evidence).
- Clean `./scripts/verify`: PASS — fmt, clippy `-D warnings`, workspace
  Rust tests (q-pack 43, q-engine-quickjs 97, q-http 11, q-bridge 11,
  q-schema-runtime 67, velqu-runtime 24), release builds, typecheck,
  proof build, Bun 81/81 with 481 expect() calls, OKF validation,
  benchmark artifact validation.
- Raw-to-report parity re-checked fresh at the candidate commit:
  route-count 12/12 cells recomputed from JSONL; codec summary cells
  all OK; evidence sha256s match raw artifacts; manifest pack hashes
  match files.
- Artifact parity: `routeCountPack10000`, bytecode pack, and proof
  artifacts match `benchmarks/manifest.json`.
- Release packet: generated by `scripts/release-packet`;
  `sha256sum -c release/SHA256SUMS.txt` passes.
- Escalated finding disposition (owner decision): the M25-010-C
  cold-start regression at 1,000 routes (~3–4x vs the G0 smoke,
  ~90% in `pack.load`) is **accepted as a documented limitation** for
  the beta track; mitigation rides the authorized M2.6 binary QPack v2
  milestone (`M26-001`). Documented in
  `docs/reports/m25-010-c-cold-start-delta.md`; not waived silently.
- No other unresolved M25 P0/P1 criteria. No unsupported performance
  claims; normative targets and measured results kept separate.
- Decision: M25 gate PASS; issue #628 may close.
