# M28 — Native Outbound Fetch

## Goal

Implement secure pooled outbound fetch with DNS/TLS, Web request/response subset, deadline/abort propagation, streaming backpressure, redirect/compression policy, SSRF controls, and fault tests.

## Task mode

Implement only after all declared dependencies pass.

## Gate rule

The milestone gate passes only when every parent evidence packet is complete, full verification passes, evidence is current, and no P0/P1 acceptance criterion remains open.

## Read limits

For an individual microtask, read this card, that task file, and only its listed source paths. Do not load the entire milestone plan.
