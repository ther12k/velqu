# Multi-Worker Context

One QuickJS runtime belongs to one worker. No JS value crosses workers. M3 introduces bounded dispatch, explicit serverless/service/throughput profiles, quarantine/replacement, adaptive scaling, fairness, and deterministic shutdown without hiding single-worker defects.
