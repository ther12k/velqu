---
task_id: M28-001-C
parent_task: M28-001
milestone: M28
priority: P0
mode: IMPLEMENT
status: PASS
context_card: context/milestones/M28.md
commit_required: true
---

# M28-001-C — Define unsupported Web features

## Atomic goal

Define unsupported Web features.

## Parent intent

Freeze the public subset, trust boundaries, defaults, and non-goals.

## Dependencies

- `M28-001-B` — `tasks/05_m28_native_fetch/M28-001-B-specify-reverse-proxy-and-outbound-trust.md`

## Read only

### Package context

- `LOW_CONTEXT_AGENT_PROMPT.md`
- `GLOBAL_INVARIANTS.md`
- `context/milestones/M28.md`
- `context/components/qpack-router.md`
- `context/components/engine-scheduler.md`
- `context/components/ingress-bridge.md`

### Source files

- `AGENTS.md`
- `crates/q-capabilities/src/lib.rs`
- `crates/q-engine-quickjs/src/worker.rs`
- `crates/q-runtime/src/serve.rs`
- `crates/q-http/src/lib.rs`
- `Cargo.toml`
- `conformance/security/security.conformance.test.ts`
- `crates/q-pack/tests/fuzz_pack.rs`
- `crates/q-http/tests/fuzz_parsers.rs`
- `crates/q-schema-runtime/tests/fuzz_validator.rs`
- `.github/workflows/verify.yml`

Do not scan unrelated directories unless a listed file imports a directly required implementation.

## Steps

1. Confirm every dependency is complete and the working tree is clean.
2. Read only the listed source files and the named milestone context card.
3. Find the existing behavior and its nearest tests; do not redesign adjacent subsystems.
4. Add or adjust the smallest test that proves this microtask when behavior or security changes.
5. Implement exactly this deliverable: Define unsupported Web features.
6. Run the targeted commands below and inspect failures rather than weakening assertions.
7. Review the diff for unrelated edits, formatting noise, generated artifacts, and hidden fallback behavior.
8. Commit one atomic change and return the required handoff.

## Parent acceptance guardrails

- Security defaults fail closed.
- Private/link-local/metadata behavior is explicit.
- Redirect revalidation is required.
- Direct TLS policy is documented.

## Targeted commands

```bash
cargo test -p q-pack
```
```bash
cargo test -p q-engine-quickjs
```
```bash
cargo test -p q-http
```
```bash
cargo test -p q-schema-runtime
```
```bash
cargo test -p q-capabilities
```
```bash
cargo test -p velqu-runtime
```
```bash
bun test
```
```bash
bun run typecheck
```

## Required evidence for this microtask

- ADR.
- Threat model.
- Security test matrix.

At minimum, the handoff must identify the exact changed files, test names, command results, and commit hash.

## Out of scope

- Other implementation bullets from the same parent task.
- Later milestones or optional features.
- Broad refactors not required by the acceptance criteria.
- Changing benchmark/report claims without raw evidence.

## Commit guidance

Suggested subject:

```text
m28-001-c: define unsupported web features
```

## Stop condition

Stop after this task is committed and handed off. Do not automatically begin the next task.

## Handoff format

Use `templates/TASK_RESULT_TEMPLATE.md`. If blocked, use `templates/BLOCKER_TEMPLATE.md`.

## Result (M28-001-C) — PASS

- Date: 2026-08-28
- Branch/PR: m28-001-c (squash-merged; see git log for final hash)
- Closes: #308

### Changed files
- `conformance/web-api/wpt-manifest.json` (v1.0.0 → v1.1.0): added the `fetch` capability —
  - Pinned subset `fetch-policy-security`: 24 executable vectors (8 scheme, 13 address-class, 3 redirect) with an explicit `verifiedIn` pointer to the Rust executor; cases carry both `allow` and `deny` directions.
  - 12 explicit skips freezing unsupported Web features with new reason codes: `NO_WEBSOCKET_BETA`, `NO_SSE_BETA`, `NO_STREAMING_REQUEST_BODIES`, `NO_FORMDATA_MULTIPART`, `NO_BLOB_FILE_TYPES`, `NO_SERVICE_WORKERS`, `NO_XHR`, `NO_HTTP2_UPSTREAM`, `NO_CLIENT_CERTS`, `NO_SOCKS_PROXY`, `NO_COOKIE_JAR`, `NO_BR_ZSTD` — each with standard reference, rationale, and deferral target (`OUT_OF_SCOPE` / `POST_M28` / `POST_BETA` / `GA_TRACK`).
- `crates/q-capabilities/tests/wpt_wintertc_conformance.rs`: new test `fetch_policy_manifest_vectors_execute_against_compiled_policy` — parses the manifest and executes every fetch vector against the compiled `FetchPolicy`, asserting expected allow/deny outcomes and requiring both directions present (≥4 allow, ≥15 deny).
- `conformance/web-api/web-api.conformance.test.ts`: capability-structure test now covers all five capabilities; valid reason-code registry extended with the 12 fetch codes; new `Fetch Security Policy Manifest (M28-001-C)` describe — executable-subset shape (id, verifiedIn pointer, both-direction cases) and frozen non-goals (WebSocket + SSE present; all skips carry deferral targets).
- `docs/reports/m27-010-wpt-wintertc-conformance.md`: regenerated by `scripts/generate-conformance-report.py` (now 58 vectors + 20 skips across 5 capabilities).

### Unsupported Web features frozen (M28 headline non-goals)
No WebSocket/SSE (`NO_WEBSOCKET_BETA`, `NO_SSE_BETA` — POST_BETA per AGENTS.md constraint 15); no Node module compatibility or filesystem/process APIs (already enforced fail-closed by COMP-006 compiler diagnostics and ADR-0003 — unchanged); no streaming request bodies, FormData, cookie jar, or br/zstd in beta; no Blob/File, Service Workers, XHR, SOCKS/HTTP proxying ever in this runtime generation; HTTP/2 upstream and client certs deferred to the GA track.

### Command results
- `cargo test -p q-capabilities` → 131 unit + 8 integration passed (was 131+7)
- `cargo test -p q-pack` 96+2 · `-p q-engine-quickjs` 16+97 · `-p q-http` 4+6+1 · `-p q-schema-runtime` 58+5+4 · `-p velqu-runtime` 31 — all pass
- `bun test` → 215 pass / 0 fail; `bun run typecheck` → clean
- `cargo fmt --check` → clean; `cargo clippy --workspace --all-targets -- -D warnings` → exit 0
- `python3 scripts/generate-conformance-report.py --check` → exit 0
- `./scripts/verify` → **ALL PASS (exit 0)**

### Guardrail mapping
- **Security defaults fail closed** — the pinned vectors execute deny expectations against the compiled policy (schemes, SSRF classes, mapped-IPv6, downgrade redirects).
- **Private/link-local/metadata behavior is explicit** — 13 address-class vectors incl. `169.254.169.254`, `fd00:ec2::235`, `::ffff:10.0.0.1`.
- **Redirect revalidation is required** — redirect vectors pinned (same-scheme allow, downgrade deny, scheme-revalidation deny).
- **Direct TLS policy is documented** — ADR-0033 §6; `NO_CLIENT_CERTS`/`NO_HTTP2_UPSTREAM` skips carry the deferral.

### Disclosures (standing)
- CI fails with zero executed steps on every PR since ~#714 (infrastructure-side); disclosed per PR. Local evidence above is complete.
