# M6-003 — Explicit unsafe / FFI Audit

Generated by `scripts/unsafe-audit.py` (reproducible; fail-closed on unclassified blocks).

## Scope and method

- Every `unsafe` fn/impl/trait/extern/block in the first-party workspace crates is enumerated below.
- Each entry is classified and paired with its covering evidence (libFuzzer target, sanitizer stage, Miri, unit tests).
- Miri exclusions (rquickjs/quickjs-ng foreign C) are compensated by the UBSan C-FFI stage (S2) — recorded in the campaign ledger, not here assumed.

## Summary

- unsafe occurrences: **21**
- crates carrying unsafe: **crates/q-bench-support, crates/q-engine-quickjs, crates/q-pack, crates/q-runtime**
- unclassified (must be zero to pass): **0**

## Inventory

| location | line | classification | snippet |
|---|---|---|---|
| `crates/q-pack/src/lib.rs` | 615 | raw memory op (mmap-backed pack load; native-only; integrity digest checked before use) | `let map = unsafe { memmap2::Mmap::map(&file) }` |
| `crates/q-engine-quickjs/src/worker.rs` | 864 | rquickjs boundary (safe-wrapped C engine; generation-checked handles) | `let module = unsafe {` |
| `crates/q-engine-quickjs/src/worker.rs` | 2768 | rquickjs boundary (safe-wrapped C engine; generation-checked handles) | `unsafe {` |
| `crates/q-engine-quickjs/src/worker.rs` | 3309 | rquickjs boundary (safe-wrapped C engine; generation-checked handles) | `unsafe {` |
| `crates/q-engine-quickjs/src/worker.rs` | 3333 | rquickjs boundary (safe-wrapped C engine; generation-checked handles) | `unsafe { std::slice::from_raw_parts_mut(buf.as_ptr() as *mut u8, buf.len()) };` |
| `crates/q-engine-quickjs/src/worker.rs` | 3396 | rquickjs boundary (safe-wrapped C engine; generation-checked handles) | `unsafe { std::slice::from_raw_parts_mut(buf.as_ptr() as *mut u8, buf.len()) };` |
| `crates/q-runtime/tests/runtime_conformance.rs` | 1916 | test-scope unsafe (covered by workspace test runs; not the serving path) | `unsafe {` |
| `crates/q-runtime/tests/runtime_conformance.rs` | 1972 | test-scope unsafe (covered by workspace test runs; not the serving path) | `unsafe {` |
| `crates/q-runtime/tests/runtime_conformance.rs` | 2088 | test-scope unsafe (covered by workspace test runs; not the serving path) | `unsafe {` |
| `crates/q-runtime/tests/runtime_conformance.rs` | 2172 | test-scope unsafe (covered by workspace test runs; not the serving path) | `unsafe {` |
| `crates/q-bench-support/src/bin/soak.rs` | 122 | FFI call (libc getrusage/dlsym allocation+CPU instrumentation; bench-support diagnostics, not the serving path) | `if unsafe { libc::getrusage(libc::RUSAGE_SELF, usage.as_mut_ptr()) } != 0 {` |
| `crates/q-bench-support/src/bin/soak.rs` | 125 | FFI call (libc getrusage/dlsym allocation+CPU instrumentation; bench-support diagnostics, not the serving path) | `let u = unsafe { usage.assume_init() };` |
| `crates/q-bench-support/src/bin/worker_scaling.rs` | 190 | FFI call (libc getrusage/dlsym allocation+CPU instrumentation; bench-support diagnostics, not the serving path) | `if unsafe { libc::getrusage(libc::RUSAGE_SELF, usage.as_mut_ptr()) } != 0 {` |
| `crates/q-bench-support/src/bin/worker_scaling.rs` | 193 | FFI call (libc getrusage/dlsym allocation+CPU instrumentation; bench-support diagnostics, not the serving path) | `let u = unsafe { usage.assume_init() };` |
| `crates/q-bench-support/src/bin/codec_bench/main.rs` | 98 | FFI call (tracer snapshot fn pointer into the allocation tracer; bench-support evidence tool, not the serving path) | `fn alloc_snapshot_fn() -> Option<unsafe extern "C" fn(*mut AllocCounters)> {` |
| `crates/q-bench-support/src/bin/codec_bench/main.rs` | 99 | FFI call (libc getrusage/dlsym allocation+CPU instrumentation; bench-support diagnostics, not the serving path) | `unsafe {` |
| `crates/q-bench-support/src/bin/codec_bench/main.rs` | 106 | FFI call (tracer snapshot fn pointer into the allocation tracer; bench-support evidence tool, not the serving path) | `unsafe extern "C" fn(*mut AllocCounters),` |
| `crates/q-bench-support/src/bin/codec_bench/main.rs` | 114 | FFI call (libc getrusage/dlsym allocation+CPU instrumentation; bench-support diagnostics, not the serving path) | `unsafe {` |
| `crates/q-bench-support/src/bin/codec_bench/main.rs` | 149 | FFI call (libc getrusage/dlsym allocation+CPU instrumentation; bench-support diagnostics, not the serving path) | `unsafe {` |
| `crates/q-bench-support/src/bin/codec_bench/main.rs` | 515 | FFI call (tracer snapshot fn pointer into the allocation tracer; bench-support evidence tool, not the serving path) | `alloc_fn: Option<unsafe extern "C" fn(*mut AllocCounters)>,` |
| `crates/q-bench-support/src/bin/codec_bench/main.rs` | 520 | FFI call (tracer snapshot fn pointer into the allocation tracer; bench-support evidence tool, not the serving path) | `unsafe { f(&mut c) };` |

## Covering evidence per crate

### `crates/q-bench-support`
- ASan workspace pass (S1, scripts/fuzz-campaign.sh)
- Miri: crate included (scripts/miri-campaign.sh)
- workspace unit tests

### `crates/q-engine-quickjs`
- ASan workspace pass (S1, scripts/fuzz-campaign.sh)
- UBSan C-FFI stage (S2, clang -fsanitize=undefined over quickjs-ng)
- Miri: excluded with reason (foreign C engine not Miri-executable)
- cargo-fuzz: bridge_handles target (stale/foreign handle invariants)
- cargo-fuzz: pack_verify target (bytecode/manifest trust boundary)
- engine conformance + settlement tests (cargo test -p q-engine-quickjs)

### `crates/q-pack`
- ASan workspace pass (S1, scripts/fuzz-campaign.sh)
- Miri: crate included (scripts/miri-campaign.sh)
- workspace unit tests

### `crates/q-runtime`
- ASan workspace pass (S1, scripts/fuzz-campaign.sh)
- Miri: crate included (scripts/miri-campaign.sh)
- workspace unit tests

